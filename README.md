# telnet-journal
personal journal script to serve entires over telnet 

serving with tcpserver via `/usr/bin/tcpserver -c 10 -HR -u 65534 -g 65534 0.0.0.0 2002 /path/to/telnet-journal.sh` on port 2002
